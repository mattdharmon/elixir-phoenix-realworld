run.config:
  engine: elixir

  dev_packages:
    - inotify-tools

  fs_watch: true

deploy.config:
  before_live:
    web.main:
      - mix ecto.create --quiet
      - mix ecto.migrate

data.db:
  image: nanobox/postgresql:9.5

web.main:
  start: mix phx.server
